<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.webproject.essuyo.dao.NoticeDao">

<insert id="create">
insert into notice (title, content, userID)
values (#{title}, #{content}, #{userID})
</insert>

<select id="read" resultType="NoticeDao">
	select noticeNum, title, registerDate, content, userID
	from notice
	where noticeNum=#{noticeNum}
</select>

<update id="update">
	update notice set tile=#{title}, content=#{content}
	where noticeNum=#{noticeNum}
</update>

<delete id="delete">
	delete from notice where noticeNum=#{noticeNum}
</delete>

<select id="listAll" resultType="NoticeVO">
<![CDATA[
	select noticeNum, title, content, userID, regDate
	from notice where noticeNum > 0
	order by noticeNum desc, regdate desc
]]>
</select>

</mapper>
