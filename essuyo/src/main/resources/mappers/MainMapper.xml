<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.webproject.essuyo.dao.MainDao">
<select id="selectOne" resultType="CompanyVO">
select *
from company;
</select>

<select id="count" resultType="Integer">
select count(*)
from company
where type=#{type};
</select>

</mapper>